// Every file in JS is know as module
// which can contains 1 line or thousands of lines

// we can access data from another file by using require, module

// when we require a sile it makes available all the modules exported from that file/module

var user3 = require("./practice10-literal-module")  // normalized path

console.log(user3);